{
  "name": "api",
  "version": "0.1.0",
  "description": "Hono API server",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev src/index.ts --port 3004",
    "build": "wrangler build",
    "preview": "wrangler preview",
    "deploy": "wrangler publish",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts,.tsx"
  },
  "dependencies": {
    "@repo/auth": "file:../../packages/auth",
    "@repo/db": "file:../../packages/db",
    "@repo/shared-types": "file:../../packages/shared-types",
    "drizzle-orm": "^0.29.0",
    "hono": "^3.11.0",
    "stripe": "^14.4.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.2",
    "wrangler": "^3.26.0"
  }
}
